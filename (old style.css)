/* 1. ПОДКЛЮЧЕНИЕ ТВОИХ ШРИФТОВ */
@font-face {
    font-family: 'DDT';
    src: url('fonts/DDTEl-Italic.woff2') format('woff2');
    font-weight: 100;
    font-style: italic;
}
@font-face {
    font-family: 'DDT';
    src: url('fonts/DDTRg-Italic.woff2') format('woff2');
    font-weight: 400;
    font-style: italic;
}
@font-face {
    font-family: 'DDT';
    src: url('fonts/DDTHv-Italic.woff2') format('woff2');
    font-weight: 900;
    font-style: italic;
}

/* 2. ПЕРЕМЕННЫЕ */
:root {
    --bg-dark: #0a0a0c;
    --bg-card: rgba(20, 20, 25, 0.6);
    --border-glass: rgba(255, 255, 255, 0.05);
    --text-white: #ffffff;
    --text-gray: #9494a6;
    --racing-red: #e62222; /* Насыщенный гоночный красный */
    --racing-red-glow: rgba(230, 34, 34, 0.3);
    --apple-red-hover: rgba(230, 34, 34, 0.15); /* Прямоугольник Apple-ховера */
    --font-main: 'DDT', sans-serif;
}

/* 3. ОБНУЛЕНИЕ СТИЛЕЙ И ЗАЩИТА ОТ ЛИШНЕГО СКРОЛЛА */
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    scroll-behavior: smooth;
}

body {
    background-color: var(--bg-dark);
    color: var(--text-white);
    font-family: var(--font-main);
    font-weight: 400;
    /* Эти две строки жестко запрещают боковой скролл сайта */
    overflow-x: hidden; 
    width: 100%;
    position: relative;
    min-height: 100vh;
    -webkit-text-size-adjust: 100%;
}

main {
    width: 100%;
    overflow: hidden; /* Дополнительный барьер для выпирающих элементов */
}

/* 4. LIQUID GLASS ФОН */
.glass-bg-blobs {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    overflow: hidden;
    pointer-events: none;
}

.blob {
    position: absolute;
    border-radius: 50%;
    filter: blur(150px);
    opacity: 0.18;
    animation: floatAnimation 14s infinite ease-in-out;
}

.blob-1 {
    top: -10%;
    right: 10%;
    width: 500px;
    height: 500px;
    background: var(--racing-red);
}

.blob-2 {
    bottom: 10%;
    left: -10%;
    width: 600px;
    height: 600px;
    background: #2563eb;
    animation-delay: -5s;
}

@keyframes floatAnimation {
    0%, 100% { transform: translateY(0) scale(1); }
    50% { transform: translateY(-30px) scale(1.08); }
}

/* 5. СТЕЛЯННЫЕ ПАНЕЛИ */
.glass-panel {
    background: var(--bg-card);
    backdrop-filter: blur(25px);
    -webkit-backdrop-filter: blur(25px);
    border: 1px solid var(--border-glass);
    border-radius: 24px;
    box-shadow: 0 12px 40px 0 rgba(0, 0, 0, 0.6);
}

/* 6. ШАПКА САЙТА */
.header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 100;
    padding: 24px 40px;
}

.header-container {
    max-width: 1400px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

/* 7. ПЕРЕКРАШИВАНИЕ ЧЕРНОГО ВЕКТОРА В КРАСНЫЙ + РЕЖИМ НАЛОЖЕНИЯ */
.logo {
    display: block;
    transition: transform 0.3s ease;
    /* Свойство смешивания цветов, чтобы лого не сливалось на темном/светлом */
    mix-blend-mode: difference; 
}

.logo img {
    height: 38px;
    display: block;
    /* Хитрый фильтр: инвертирует черный в белый, сдвигает в красный сепией 
       и накладывает сочный гоночный оттенок */
    filter: invert(21%) sepia(93%) saturate(6935%) hue-rotate(356deg) brightness(94%) contrast(117%) drop-shadow(0 0 1px rgba(255,255,255,0.2));
}

.logo:hover {
    transform: scale(1.03);
}

/* 8. Меню БЕЗ ФОНА */
.ios-menu {
    display: flex;
    background: rgba(255, 255, 255, 0.02);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.05);
    padding: 4px;
    border-radius: 14px;
}

.menu-item {
    position: relative;
}

.menu-link {
    display: block;
    color: var(--text-white);
    text-decoration: none;
    padding: 10px 24px;
    font-size: 16px;
    font-weight: 400;
    border-radius: 10px;
    transition: background-color 0.25s ease, color 0.25s ease;
}

/* Эппловский ховер с появлением красного прямоугольника прямо на кнопке */
.menu-link:hover {
    background-color: var(--apple-red-hover);
    color: var(--racing-red);
}

/* iOS Выпадающий попап */
.dropdown-popup {
    position: absolute;
    top: calc(100% + 14px);
    left: 50%;
    transform: translateX(-50%) translateY(10px);
    width: 250px;
    background: rgba(14, 14, 18, 0.94);
    backdrop-filter: blur(30px);
    -webkit-backdrop-filter: blur(30px);
    border: 1px solid var(--border-glass);
    border-radius: 18px;
    padding: 8px;
    box-shadow: 0 16px 50px rgba(0,0,0,0.8);
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.25s ease, transform 0.25s cubic-bezier(0.16, 1, 0.3, 1), visibility 0.25s;
}

@media (min-width: 769px) {
    .has-popup:hover .dropdown-popup {
        opacity: 1;
        visibility: visible;
        transform: translateX(-50%) translateY(0);
    }
}

.menu-item.active-click .dropdown-popup {
    opacity: 1;
    visibility: visible;
    transform: translateX(-50%) translateY(0);
}

.dropdown-popup a {
    display: block;
    color: var(--text-white);
    text-decoration: none;
    padding: 12px 18px;
    font-size: 15px;
    border-radius: 12px;
    transition: background-color 0.2s ease;
}

.dropdown-popup a:hover {
    background-color: rgba(255, 255, 255, 0.04);
    color: var(--racing-red);
}
/* 9. ГЛАВНЫЙ ЭКРАН (HERO) */
.hero {
    max-width: 1400px;
    margin: 150px auto 60px;
    padding: 0 40px;
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 40px;
    align-items: center;
    width: 100%;
}

.hero-content {
    padding: 60px;
}

.hero-title {
    font-size: 72px;
    font-weight: 900;
    letter-spacing: -1px;
    margin-bottom: 24px;
    text-transform: uppercase;
    background: linear-gradient(135deg, #ffffff 30%, #888896 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.hero-subtitle {
    font-size: 18px;
    line-height: 1.6;
    color: var(--text-gray);
    margin-bottom: 40px;
}

/* 10. КНОПКИ */
.hero-actions {
    display: flex;
    gap: 16px;
}

.btn {
    display: inline-block;
    text-decoration: none;
    padding: 16px 32px;
    font-size: 15px;
    font-weight: 900;
    border-radius: 14px;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    text-transform: uppercase;
    white-space: nowrap;
}

.btn-primary {
    background: var(--racing-red);
    color: var(--text-white);
    box-shadow: 0 4px 20px var(--racing-red-glow);
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 26px var(--racing-red);
}

.btn-secondary {
    background: rgba(255, 255, 255, 0.04);
    color: var(--text-white);
    border: 1px solid var(--border-glass);
}

.btn-secondary:hover {
    background: rgba(255, 255, 255, 0.08);
    transform: translateY(-2px);
}

/* Элемент-заглушка для видео/промо */
.hero-image-placeholder {
    height: 460px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-gray);
    position: relative;
    overflow: hidden;
    text-align: center;
}

.pulse-glow {
    position: absolute;
    width: 60%;
    height: 60%;
    background: var(--racing-red-glow);
    filter: blur(70px);
    border-radius: 50%;
    animation: pulse 4s infinite alternate ease-in-out;
}

@keyframes pulse {
    0% { transform: scale(0.9); opacity: 0.3; }
    100% { transform: scale(1.1); opacity: 0.6; }
}

/* 11. СЕКЦИЯ ТАРИФОВ (ОБЩЕЕ) */
.tariffs-section {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 40px;
    width: 100%;
}

.section-title {
    font-size: 36px;
    font-weight: 900;
    margin: 80px 0 32px;
    text-transform: uppercase;
    border-left: 5px solid var(--racing-red);
    padding-left: 18px;
    letter-spacing: 0.5px;
}

/* 12. ДЕСКТОПНАЯ ТАБЛИЦА ЦЕН */
.table-responsive {
    width: 100%;
    background: rgba(16, 16, 20, 0.45);
    border: 1px solid var(--border-glass);
    border-radius: 20px;
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.5);
}

.tariffs-table {
    width: 100%;
    border-collapse: collapse;
    text-align: center;
}

.tariffs-table th, 
.tariffs-table td {
    padding: 22px 24px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
}

.th-main-title {
    text-align: left;
    font-size: 14px;
    text-transform: uppercase;
    color: var(--text-gray);
    font-weight: 400;
    width: 28%;
}

.th-group-days {
    font-size: 16px;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--text-gray);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08) !important;
    padding: 16px;
}

.th-group-days.red-title {
    color: var(--racing-red);
}

.tariffs-table th {
    font-size: 14px;
    color: #646473;
    font-weight: 400;
}

.tariffs-table th.red-cell-title {
    color: rgba(230, 34, 34, 0.7);
}

.tariff-info {
    text-align: left;
}

.tariff-name {
    display: block;
    font-size: 24px;
    font-weight: 900;
    letter-spacing: 0.5px;
    margin-bottom: 4px;
}

.tariff-sub {
    display: block;
    font-size: 13px;
    color: var(--text-gray);
}

.price-cell {
    font-size: 26px;
    font-weight: 900;
    color: var(--text-white);
}

.red-cell {
    color: var(--racing-red);
    background: rgba(230, 34, 34, 0.015);
}

.tariffs-table tbody tr:hover {
    background: rgba(255, 255, 255, 0.015);
}

/* 13. СЕТКА КОМБО-НАБОРОВ */
.tariffs-grid {
    display: grid;
    gap: 30px;
    width: 100%;
}
.tariffs-grid.double { grid-template-columns: repeat(2, 1fr); }

.tariff-card {
    padding: 40px;
    display: flex;
    flex-direction: column;
    transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1), border-color 0.3s ease;
}

.tariff-card:hover {
    transform: translateY(-4px);
    border-color: rgba(230, 34, 34, 0.3);
}

.tariff-card h3 {
    font-size: 28px;
    font-weight: 900;
    margin-bottom: 16px;
}

.tariff-card .price {
    font-size: 34px;
    font-weight: 900;
    margin-bottom: 16px;
}

.tariff-card .price span {
    font-size: 16px;
    font-weight: 400;
    color: var(--text-gray);
}

.tariff-card .desc {
    color: var(--text-gray);
    line-height: 1.5;
    font-size: 15px;
}
/* 14. СТИЛИ ДЛЯ БОЛЬШИХ САМОСТОЯТЕЛЬНЫХ БЛОКОВ (МЕРОПРИЯТИЯ) */
.large-info-section {
    width: 100%;
    max-width: 1400px;
    margin: 100px auto;
    padding: 0 40px;
}

.large-info-container {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 40px;
    align-items: stretch;
    width: 100%;
}

.large-info-content {
    padding: 60px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
}

.block-main-title {
    font-size: 42px;
    font-weight: 900;
    text-transform: uppercase;
    margin-bottom: 24px;
    letter-spacing: -0.5px;
}

.block-main-desc {
    font-size: 16px;
    line-height: 1.7;
    color: var(--text-gray);
    margin-bottom: 40px;
}

/* Элемент для изображений внутри инфо-блоков */
.large-info-image {
    position: relative;
    overflow: hidden;
    min-height: 380px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.01);
}

.large-info-image img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover; /* Картинка заполняет весь блок без искажений */
    transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1);
}

.large-info-container:hover .large-info-image img {
    transform: scale(1.04);
}

.image-stub {
    color: var(--text-gray);
    font-size: 14px;
    z-index: 1;
}

/* Чередование блоков (шахматный порядок для десктопа) */
.large-info-section.reverse .large-info-container {
    grid-template-columns: 0.9fr 1.1fr;
}

.large-info-section.reverse .large-info-container .large-info-content {
    grid-order: 2; /* Меняем порядок контента на десктопе */
    order: 2;
}

.large-info-section.reverse .large-info-container .large-info-image {
    grid-order: 1;
    order: 1;
}

/* Анимация плавного появления */
.animate-fade-in {
    animation: fadeIn 1s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

@keyframes fadeIn {
    from { opacity: 0; transform: translateY(15px); }
    to { opacity: 1; transform: translateY(0); }
}


/* ================= ПОЛНЫЙ МОБИЛЬНЫЙ АДАПТИВ ================= */

@media (max-width: 1100px) {
    .hero {
        grid-template-columns: 1fr;
        margin-top: 140px;
        gap: 30px;
    }
    .hero-content {
        padding: 40px;
    }
    .hero-image-placeholder {
        height: 350px;
    }
    .large-info-container,
    .large-info-section.reverse .large-info-container {
        grid-template-columns: 1fr;
    }
    .large-info-section.reverse .large-info-container .large-info-content {
        order: 1;
    }
    .large-info-section.reverse .large-info-container .large-info-image {
        order: 2;
    }
}

@media (max-width: 768px) {
    /* Тотальный сброс и защита от бокового скролла */
    body, html {
        overflow-x: hidden;
        width: 100%;
    }
    
    .header {
        padding: 16px 20px;
    }
    
    /* Шапка: логотип слева, iOS меню справа */
    .header-container {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        align-items: center;
        width: 100%;
    }

    .logo img {
        height: 32px;
    }
    
    .menu-link {
        padding: 10px 18px;
        font-size: 15px;
    }

    /* Смещаем мобильный попап к правому краю, чтобы он не улетал за экран */
    .dropdown-popup {
        position: absolute;
        top: calc(100% + 10px);
        right: 0;
        left: auto;
        transform: translateY(10px);
        width: 220px;
    }

    .hero {
        padding: 0 20px;
    }

    .hero-title {
        font-size: 46px;
    }

    .hero-subtitle {
        font-size: 16px;
        margin-bottom: 30px;
    }

    .hero-actions {
        flex-direction: column;
        gap: 12px;
        width: 100%;
    }

    .btn {
        width: 100%;
        text-align: center;
    }

    /* ТРАНСФОРМАЦИЯ ТАБЛИЦЫ ЦЕН В МОБИЛЬНЫЕ КАРТОЧКИ */
    .tariffs-section {
        padding: 0 20px;
    }

    /* Полностью скрываем десктопную структуру таблицы */
    .tariffs-table thead {
        display: none;
    }

    .tariffs-table, 
    .tariffs-table tbody, 
    .tariffs-table tr, 
    .tariffs-table td {
        display: block;
        width: 100%;
    }

    .table-responsive {
        background: transparent;
        border: none;
        box-shadow: none;
    }

    /* Превращаем каждую строку таблицы в отдельную красивую iOS-плашку */
    .tariffs-table tbody tr {
        background: var(--bg-card);
        border: 1px solid var(--border-glass);
        border-radius: 20px;
        margin-bottom: 24px;
        padding: 24px;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
    }

    .tariffs-table tbody tr:hover {
        background: var(--bg-card);
    }

    .tariff-info {
        text-align: center;
        border-bottom: 1px solid rgba(255, 255, 255, 0.06);
        padding-bottom: 16px !important;
        margin-bottom: 16px;
    }

    .tariff-name {
        font-size: 26px;
        text-align: center;
    }

    .tariff-sub {
        text-align: center;
        margin-top: 4px;
    }

    /* Стилизуем ячейки с ценами как список параметров внутри карточки */
    .price-cell {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 10px 0 !important;
        font-size: 20px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.03) !important;
    }

    /* Добавляем текстовые подписи перед ценами, которых не было на десктопе */
    .price-cell:nth-child(2)::before { content: "ПН-ЧТ (30 мин):"; font-size: 14px; font-weight: 400; color: var(--text-gray); }
    .price-cell:nth-child(3)::before { content: "ПН-ЧТ (60 мин):"; font-size: 14px; font-weight: 400; color: var(--text-gray); }
    .price-cell:nth-child(4)::before { content: "ПН-ЧТ (120 мин):"; font-size: 14px; font-weight: 400; color: var(--text-gray); }
    
    .price-cell:nth-child(5)::before { content: "ПТ-ВС (30 мин):"; font-size: 14px; font-weight: 400; color: var(--text-gray); }
    .price-cell:nth-child(6)::before { content: "ПТ-ВС (60 мин):"; font-size: 14px; font-weight: 400; color: var(--text-gray); }
    .price-cell:nth-child(7)::before { content: "ПТ-ВС (120 мин):"; font-size: 14px; font-weight: 400; color: var(--text-gray); }

    .price-cell.red-cell {
        background: transparent;
    }

    .price-cell:last-child {
        border-bottom: none !important;
    }

    /* Корректировка сеток комбо и блоков мероприятий */
    .tariffs-grid.double {
        grid-template-columns: 1fr;
        gap: 20px;
    }

    .tariff-card {
        padding: 30px 24px;
    }

    .tariff-card h3 { font-size: 24px; }
    .tariff-card .price { font-size: 28px; }

    .large-info-section {
        padding: 0 20px;
        margin: 60px auto;
    }

    .large-info-content {
        padding: 32px 24px;
        align-items: center;
        text-align: center;
    }

    .block-main-title {
        font-size: 32px;
    }

    .block-main-desc {
        font-size: 15px;
        margin-bottom: 30px;
    }

    .large-info-image {
        min-height: 260px;
    }
}
